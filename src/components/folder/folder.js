import angular from 'angular';
import folderComponent from './folder.component';

let folderModule = angular.module('folder', [

])
  .component('folder', folderComponent)
  .name

export default folderModule;