import angular from 'angular';
import titlebarComponent from './titlebar.component';

let titlebarModule = angular.module('titlebar', [
])
  .component('titlebar', titlebarComponent)
  .name

export default titlebarModule;