import template from './drive.html';
import controller from './drive.controller';

let driveComponent = {
  bindings: {
    details: "="
  },
  template,
  controller
}

export default driveComponent;