import angular from 'angular';
import folderComponent from './directoryItem.component';

let folderModule = angular.module('directoryItem', [

])
  .component('directoryItem', folderComponent)
  .name

export default folderModule;